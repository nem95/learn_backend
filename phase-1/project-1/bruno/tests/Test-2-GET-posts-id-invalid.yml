info:
  name: Test 2 - GET /posts/999 (invalide)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/posts/999"
  headers:
    - name: Content-Type
      value: application/json

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 404 (Not Found)", function() {
          expect(res.status).to.equal(404);
        });
