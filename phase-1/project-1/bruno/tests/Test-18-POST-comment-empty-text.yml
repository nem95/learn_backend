info:
  name: Test 18 - POST /posts/:id/comments (texte vide → 400)
  type: http
  seq: 19

http:
  method: POST
  url: "{{baseUrl}}/posts/{{postId}}/comments"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "text": ""
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 400 (Bad Request)", function() {
          expect(res.status).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
