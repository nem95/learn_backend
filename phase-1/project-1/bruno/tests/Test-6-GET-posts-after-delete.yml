info:
  name: Test 6 - GET /posts (après suppression)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/posts"
  headers:
    - name: Content-Type
      value: application/json

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("Posts array is empty", function() {
          expect(res.body).to.be.an("array");
          expect(res.body).to.have.lengthOf(0);
        });
